// Defines Trackbox's global API and private data namespaces
var tb = {};
tb.private = {};

// Namespace for other packages
// To avoid naming conflicts, make your package extend this with your GitHub name and then extend that with your package's name.
var packages = {};